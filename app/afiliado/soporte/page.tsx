import { redirect } from 'next/navigation';
import { Headphones, ShieldCheck, Clock3 } from 'lucide-react';
import Link from 'next/link';
import { createClient } from '@/lib/supabase/server';
import { PlatformShell, PageHeader } from '@/components/platform-shell';
import SupportChat from '@/components/support-chat';
export const dynamic='force-dynamic';
export default async function AffiliateSupportPage(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/');const {data:profile}=await s.from('profiles').select('full_name,role,status').eq('id',user.id).maybeSingle();if(!profile||profile.role!=='affiliate'||profile.status!=='active')redirect('/');return <PlatformShell role="affiliate" name={profile.full_name||'Afiliado'}><PageHeader eyebrow="SOPORTE" title="Estamos aquí para ayudarte" description="Habla directamente con Administración cuando tengas una duda sobre ventas, retiros, productos o tu formación." action={<Link href="/afiliado" className="native-secondary">Volver al inicio</Link>}/><div className="support-intro-grid"><section className="support-intro-card support-intro-primary"><div className="support-intro-icon"><Headphones size={20}/></div><div><span className="section-kicker">SOPORTE DIRECTO</span><h2>Tu consulta llega al administrador</h2><p>Escribe tu duda en el chat. Al iniciar la conversación recibirás un saludo automático y podrás continuar escribiendo normalmente.</p></div></section><section className="support-trust-card"><div><ShieldCheck size={18}/><strong>Conversación privada</strong><span>Solo tú y Administración pueden verla.</span></div><div><Clock3 size={18}/><strong>Respuesta humana</strong><span>Te responderemos por este mismo chat.</span></div></section></div><SupportChat mode="affiliate"/></PlatformShell>}
